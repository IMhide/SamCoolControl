# Coolify API config pour CETTE infra (copier en .env, gitignoré)
# Le token se génère depuis l'UI Coolify : Keys & Tokens > API tokens (scopes read/write/deploy)

# URL de l'instance Coolify (sans slash final)
COOLIFY_BASE_URL=https://<coolify>

# Token API
COOLIFY_API_TOKEN=
