Display a full status dashboard of the Coolify server.

Run `./scripts/coolify status` and present the results in a clear, organized way. Highlight any issues (unreachable servers, failed deployments, stopped services).
